# 上下文五层架构

```
指令层 → 系统规则，不可变（Role + 死规矩 + 输出格式）
任务层 → 用户原始输入（可能模糊或恶意，不可直接信任）
事实层 → 链上 API 返回（实时，可验证，铁证）
知识层 → RAG 搜到的审计/文档/黑名单（标来源+日期+版本）
外部层 → dApp 页面文案 / 项目方宣传（⚠️ 默认当诈骗话术，拿链上数据打脸）
```

## 原则

- 每样东西塞给模型前，先标"你是谁、我能信你多少"
- 外部不可信内容放最底层，链上数据优先级最高
- 知识库内容过期就标出来，不能当永远正确用
